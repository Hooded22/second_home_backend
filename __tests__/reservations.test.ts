describe("Reservation CRUD", () => {
    test.todo("GET /reservation --> 200, all dta");
    test.todo("GET /reservation with filters --> 200, all data fit filters");
    test.todo("GET /reservation by user with 'customer' role --> 403, permission denied");
    test.todo("GET /reservation/myReservations by user with 'customer' role --> 200");
    test.todo("GET /reservation/myReservations --> data assigne to requesting user");
})